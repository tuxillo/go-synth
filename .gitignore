# Binaries
dsynth
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Build output directories (runtime, not source)
# Note: build/ is a source directory, so it should NOT be ignored
/logs/
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Config (user-specific)
/etc/dsynth/
*.db
# Original C source (local reference only)
.original-c-source/
